@model MeetingApp.Models.UserInfo
@{
    ViewBag.Title = "Başvuru Detayı";
}
@{
    var className = @Model.WillAttend == true ? "success" : "danger";
}

<main class="text-center">
    <h1 class="h4">Başvuru Detayı</h1>
    <p>
        <span class="badge text-bg-@className">
            @if (Model.WillAttend == true)
            {
                <text>Katılıyor</text>
            }
            else
            {
                <text>Katılmıyor</text>
            }
        </span>
    </p>
    <p class="">İsim: @Model.Name</p>
    <p class="">Email: @Model.Email</p>
    <p class="">Telefon: @Model.Phone</p>
    <a asp-controller="Meeting" asp-action="List" class="btn btn-outline-@className">Katılımcı Listesi</a>
    
</main>